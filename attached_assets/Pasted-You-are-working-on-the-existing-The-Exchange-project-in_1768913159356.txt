You are working on the existing “The Exchange” project in Replit.

TASK TYPE: Data + default flow refactor (mock data + default requirements)
IMPORTANT: Do NOT redesign the UI layout. Keep the current screens, styling, and navigation. Only update the mock data, default document requirements, and the logic that powers the “Document Library” and property document checklist behaviour.

Goal: Remove all existing mock property/client data and replace it with ONE new example property that demonstrates the most up-to-date tenancy onboarding flow, based on the provided “Tenancy Paperwork Checklist” (Property Stop example). This new example must be the default baseline flow, but must remain configurable per property/agency so agents can edit the list (add/remove/customise requirements) as they currently can.

DO NOT write backend code yet. This is still mock-data / prototype logic work.

------------------------------------------------------------
WHAT TO DELETE / REPLACE
------------------------------------------------------------
1) Delete all existing mock properties, clients, documents, rent schedule examples, chats, and activity items currently seeded for demo.
2) Replace them with one new example property and associated example client(s) that demonstrate the full checklist flow.
3) Any existing demo pages that rely on mock data should continue to work, but with the new dataset.

------------------------------------------------------------
NEW DEFAULT EXAMPLE PROPERTY (SINGLE PROPERTY)
------------------------------------------------------------
Create exactly ONE example property with:
- a realistic property name and address (fictional)
- a realistic rent amount
- status that makes sense in onboarding (e.g., “In Review”)
- a single example tenant client linked to the property
- include an example “guarantor required” scenario (so the guarantor section can be demonstrated)

The property overview should still show:
- document checklist
- client details
- rent schedule (can be placeholder but must be coherent)
- recent activity (update to reflect the new checklist actions)
- quick actions can remain but update labels if needed to match the new flow (no UI redesign)

------------------------------------------------------------
DEFAULT DOCUMENT CHECKLIST FLOW (BASELINE TEMPLATE)
------------------------------------------------------------
Update the default required documents/checklist items to match this baseline structure (based on the tenancy paperwork checklist). Represent each requirement as a checklist item that can be:
- required/optional
- assigned to tenant or guarantor (where relevant)
- given a status: Missing / Uploaded / In Review / Approved (or your current status system)

Baseline checklist sections and items:

A) Application
- Signed tenancy application form (required)

B) Tenant identity
- Tenant photographic ID (passport or driving licence) (required)

C) Tenancy history
- Previous tenancy agreement (required)
  - must cover landlord name/address, tenancy period, property address, rental amount (store these as sub-requirements or notes)
- Previous landlord reference / permission evidence (optional/required toggle)

D) Tenancy agreement
- Signed tenancy agreement (all pages signed, last page signed & dated) (required)

E) Affordability / status
- Employment contract OR last 3 months payslips (required if “Employed”)
- Student ID OR acceptance/enrolment documents (required if “Student”)

F) Proof of address (Tenant)
- Proof of address document 1 (e.g., bank statement within last 3 months) (required)
- Proof of address document 2 (e.g., utility bill or driving licence) (required)

G) Guarantor (only if required for this property)
- Completed guarantor form (required)
- Guarantor proof of home ownership (required)
- Guarantor photographic ID (required)

H) Payments evidence
- Proof of balance of rent payment (required)
- Evidence of damage deposit standing order / payment (required)
- Evidence of standing order set up for future rent payments (required)

I) Bank details / payment instructions (display-only)
- Bank details section visible to client (not an upload requirement)
- Reference field displayed

Notes:
- Keep wording concise inside the UI, but preserve the meaning.
- Where the baseline document implies “emailing landlord”, treat it as a trackable evidence item (upload/email proof) rather than automating sending emails.

------------------------------------------------------------
DOCUMENT LIBRARY PAGE CHANGES
------------------------------------------------------------
Update the Document Library page so that:
1) The “default document list” reflects the above baseline template.
2) Existing functionality for agents to edit the default list remains:
   - agents can add new requirement items
   - agents can remove or mark items optional
   - agents can reorder items (if supported)
   - agents can create custom categories/sections (if supported)
3) The baseline template should be represented in the same data structure the current system uses for document requirements, so it remains editable.
4) If the system currently supports per-property overrides, ensure this still works:
   - the example property starts with the default template applied
   - the agent can modify requirements for that property without breaking the global default

------------------------------------------------------------
MOCK DOCUMENTS & STATUS EXAMPLES
------------------------------------------------------------
For the new example property, include a realistic mix of statuses:
- a few items already uploaded (e.g., application form, tenant ID)
- a few items “in review” (e.g., previous tenancy agreement)
- several still missing (e.g., guarantor home ownership proof)
- include at least one “approved” item to show the end state

Each uploaded item should have:
- filename
- uploaded_by (tenant/agent/guarantor)
- uploaded date
- optional notes

Keep all names and documents fictional.

------------------------------------------------------------
RENT SCHEDULE (COHERENT EXAMPLE)
------------------------------------------------------------
Update the rent schedule to be coherent and connected to the example property:
- show next payment date(s) and amounts
- include deposit line if your UI supports it
- do not include real bank numbers or real references

------------------------------------------------------------
RECENT ACTIVITY (UPDATE TO MATCH NEW FLOW)
------------------------------------------------------------
Replace the existing recent activity items with ones that reflect this onboarding:
- “Tenancy application submitted”
- “Photographic ID uploaded”
- “Previous tenancy agreement uploaded”
- “Guarantor requested”
- “Deposit payment evidence uploaded”
etc.

------------------------------------------------------------
QUALITY REQUIREMENTS
------------------------------------------------------------
- Remove all old mock data cleanly (no unused references).
- Ensure all relevant pages still load without errors.
- Ensure document checklist and document library are consistent with each other.
- Keep the UI looking the same; only change content and underlying mock data structures.
- Keep the default checklist configurable/editable by the agent as it is now.

DELIVERABLE:
Update the project so it contains one new example property and a baseline, editable tenancy onboarding document flow matching the checklist above, with the Document Library updated accordingly.
